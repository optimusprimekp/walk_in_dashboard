# PostgreSQL connection string
DATABASE_URL=postgres://walkin:CHANGE_ME@localhost:5432/walkin

# Port the API/server listens on (defaults to 3001)
PORT=3001

# Set to "production" on the server so logs are JSON and the built client is served
NODE_ENV=production

# Admin seed credentials (used by `npm run db:seed`)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@kp2026
ADMIN_NAME=Administrator
